# 提交分支 submission/c2：本分支只保留赛题二(c2)的工程代码与文档。
# c1 / c3 / c4 是其它赛题，已从本分支移出（源码仍在 main 分支）；
# 下面的忽略项让本地遗留的它们的产物不干扰 git status。
/c1/
/c3/
/c4/

# ============ Python ============
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# ============ 环境 ============
.venv/
venv/
env/

# ============ 原始数据集（主办方发布，不随代码提交）============
**/data/
*.jpg
*.jpeg
*.png

# ============ c2 运行期产物（可由代码重建，不随代码提交）============
# FinixDoc-VL 返回结果缓存(按图像内容哈希)
c2/cache/
# 上采样 tile 的结果缓存
c2/cache_up/
# 纯几何计算缓存
c2/cache_geo/
# 格级本地 OCR 结果缓存(sqlite + -wal/-shm)
c2/rec_cache.sqlite*
# 提交 CSV、运行日志、本地评测结果、审计页
c2/out/

# ============ 编辑器 / IDE ============
.vscode/
.idea/

# ============ 临时/系统 ============
*.tmp
*.log.*
.DS_Store
