# 核心 Agent 依赖（推理/检索/解析）
pymupdf>=1.24
beautifulsoup4>=4.12
lxml>=5.0
rank_bm25>=0.2.2
jieba>=0.42
numpy>=1.26
openai>=1.40            # 兼容百炼/魔搭 Qwen OpenAI-compatible API
tqdm>=4.66
pyyaml>=6.0

# 预处理增强（可选；MinerU 较重，单独装见 README）
# mineru[core]          # 扫描件/复杂表格 fallback，建议 pip install "mineru[core]"
# paddleocr             # 轻量 OCR 备选
